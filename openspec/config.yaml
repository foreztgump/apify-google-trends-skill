schema: spec-driven

context: |
  Project: apify-google-trends-skill
  Overview: Agent Skill (agentskills.io standard) that wraps the Apify Google Trends Scraper
    actor (apify/google-trends-scraper). Provides AI agents with a clean interface to query
    Google Trends data — interest over time, regional interest, related topics, and related
    queries. Uses direct Apify REST API calls via httpx (no SDK).
  Stack: Python 3.12+, httpx 0.28.1, pydantic 2.12.5
  API: Apify REST API v2 — POST /v2/acts/{actorId}/run-sync-get-dataset-items for
    synchronous runs, or POST /v2/acts/{actorId}/runs + GET /v2/actor-runs/{runId}/dataset/items
    for async polling.
  Actor: apify/google-trends-scraper — accepts searchTerms, timeRange, geo, category,
    isMultiple, viewedFrom, startUrls, customTimeRange, maxItems, skipDebugScreen.
  Output: Each dataset item contains searchTerm, interestOverTime_timelineData,
    interestBySubregion, interestByCity, interestBy, relatedTopics_top, relatedTopics_rising,
    relatedQueries_top, relatedQueries_rising.
  Structure: src/apify_google_trends_skill/ (package), tests/ (pytest + respx),
    SKILL.md (agent skill entry point), scripts/ (executable scripts for agents).
  Testing: pytest + respx for mocking httpx. Fixtures with realistic Apify response payloads.
  Security: API token via APIFY_API_TOKEN env var only. Never hardcode tokens.
  Deployment: Installed as Agent Skill folder in any compatible agent's skills directory.

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then format
  design:
    - Include sequence diagrams for complex flows
    - Explicitly evaluate module depth — prefer deep modules (simple interface, complex implementation)
    - Address information hiding — what design decisions does each module encapsulate?
    - Consider at least two design approaches before committing (design it twice)
    - Document dependency direction — high-level modules depend on abstractions
  tasks:
    - Each task < 2 hours
    - Include acceptance criteria
    - Specify test files to create/update
    - Include applicable code quality constraints from CODE_PRINCIPLES.md
    - Specify error handling strategy for the task's scope
  code:
    - Functions do one thing — if a function needs "and" in its description, split it
    - No magic values — all literals that aren't self-evident must be named constants
    - Prefer deep modules — simple interface, complex implementation hidden behind it
    - Error handling is mandatory for all async operations and external boundaries
    - Names reveal intent — no abbreviations, no generic names (data, info, item, temp)
    - Maximum 3 levels of nesting — use early returns, extraction, or guard clauses
    - Comments explain WHY, not WHAT — code should be self-documenting for the what
    - No duplicated logic — extract shared behavior into functions or modules
    - KISS — pick the simplest solution that works; no unnecessary patterns or indirection
    - YAGNI — only build what the task requires; no speculative abstractions
    - Law of Demeter — talk to direct collaborators only; no chaining through objects
    - Composition over inheritance — combine objects, don't extend class hierarchies
